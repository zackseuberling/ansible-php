1. Duplicate example files into usuable forms:

```
$ cp playbook.example playbook.yml && cp hosts.example hosts
```

2. Update relevant information for your servers

3. Run `ansible-playbook playbook.yml`
